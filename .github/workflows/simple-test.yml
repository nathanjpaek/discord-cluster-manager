name: Simple Test
on:
  workflow_dispatch:

jobs:
  test:
    runs-on: self-hosted
    steps:
      - name: Print Hello
        run: |
          echo "Hello from GitHub Actions!"
          python3 --version
          nvidia-smi || echo "No GPU found"


